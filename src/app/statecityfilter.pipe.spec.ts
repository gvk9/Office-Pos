import { StatecityfilterPipe } from './statecityfilter.pipe';

describe('StatecityfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatecityfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
